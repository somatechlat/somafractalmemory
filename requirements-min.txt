# Minimal runtime requirements for quick local testing
# Removes heavy ML packages (torch, transformers) to keep image small
fastapi
uvicorn
httpx
# Core libs
numpy==1.26.4
redis>=4.5.1
qdrant-client>=1.2,<2
scikit-learn==1.3.2
cryptography==41.0.3
prometheus-client==0.16.0
tenacity==8.2.2
structlog==23.1.0
Pillow==9.5.0
networkx==3.1
requests>=2.31.0
# note: pytest, fakeredis and other test deps intentionally omitted

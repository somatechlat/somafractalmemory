# Core settings
namespace: "default"
vector_dim: 768
api_port: 10101

# Infrastructure endpoints
infra:
  redis: "redis"
  vault: ""
  opa: ""
  auth: ""
  etcd: ""
  prometheus: ""
  jaeger: ""
  milvus: "milvus"
  postgres: "postgres"

# Database connections
# Keep passwords out of the repo; supply via env/secret in the actual deployment.
postgres_url: "postgresql://soma@postgres:5432/somamemory"
milvus_host: "milvus"
milvus_port: 19530

name: doc-health

on:
  workflow_dispatch: {}
  push:
    paths:
      - 'docs/**'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install linters
        run: |
          python -m pip install --upgrade pip
          pip install markdownlint-cli httpx
      - name: Run markdownlint (draft)
        run: |
          # draft: requires configuration review
          echo "Running markdown check (placeholder)"
      - name: link-check (draft)
        run: |
          echo "Link check placeholder"

  build-report:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - name: Generate doc-health report (draft)
        run: |
          echo "doc-health report placeholder" > docs/doc-health-report.txt

# Development override: run API on 9999 (temporary) without disturbing existing 9595 or 9696 usages.
# Use when 9595 is serving external users and 9696 reserved for brain work; ephemeral alternate.
service:
  port: 9999

env:
  UVICORN_PORT: "9999"
  SOMA_RATE_LIMIT_MAX: "5000"

probe:
  port: 9999
  path: /healthz

# Matches pattern of values-dev-port9696.yaml; no persistence toggles here.

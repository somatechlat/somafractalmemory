apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-somafractalmemory-secrets
type: Opaque
data:
  SOMA_API_TOKEN: {{ .Values.secrets.apiToken | b64enc | quote }}
  POSTGRES_PASSWORD: {{ .Values.secrets.postgresPassword | b64enc | quote }}
